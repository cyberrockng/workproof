# Dependency Changes

Scaffold base: `ffb6c4ca7c160c49be59e00fe537e24d2477b000`

Use this table for every future change:

| Date | Area | Package | Previous | New | Reason | Validation |
|---|---|---|---|---|---|---|
| 2026-08-08 | Solidity (test-only) | `foundry-rs/forge-std` (git submodule, `lib/forge-std`) | not present | v1.16.2 (`bf647bd6046f2f7da30d0c2bf435e5c76a780c1b`) | Needed `vm.sign`/`vm.prank`/`vm.expectRevert`/`vm.warp`/`bound` cheatcodes and `Test` assertions to build the Phase 2 required-simulation suite and the FCC signature-compatibility spike; not linked into any production contract. | `forge test`: 29/29 pass (`test/FccSignatureSpike.t.sol`, `test/WorkProofEscrow.t.sol`); `forge build`/`forge fmt --check` clean. |
| 2026-08-08 | Solidity (production) | `flare-foundation/flare-foundry-periphery-package` (git submodule, `lib/flare-foundry-periphery-package`, includes its own pinned `@openzeppelin-contracts@5.2.0-rc.1` via soldeer) | not present | tag `0.1.52` (`ca264d6a31ddfb53d1bef7cb7bd1942aa89d323a`) | Phase 3 production contract needs real `IFlareContractRegistry`/`ContractRegistry`/`IAssetManager`/`IRelay`/`RandomNumberV2Interface` types to resolve FTestXRP and secure randomness live from the chain (plan section 10 "Address resolution") instead of pasting addresses as constants, plus real `SafeERC20`/`ReentrancyGuard`. Same package + version already used and audited-in-practice by the sibling Ajose project. | `forge build`: clean; `forge test`: 78/78 local + 7/7 real-Coston2-fork pass, incl. `testConstructorResolvesRealFxrpRelayAndRandomNumberV2` matching the exact real FTestXRP/Relay addresses. |
| 2026-08-09 | Go (production, direct) | `gowebpki/jcs` | not present | v1.0.1 | Phase 4's `go/internal/verifier/bundle.go` needs a real RFC 8785 (JSON Canonicalization Scheme) implementation to canonicalize the decrypted bundle before hashing into `privateBundleHash`, and to canonicalize `PublicReport` into `reportHash` — both are on-chain commitments the escrow's `_checkOutcomeBinding` verifies exactly, so a hand-rolled or non-standard canonicalizer would silently produce a different hash than any other RFC-8785-compliant implementation a client might use to construct the original commitment. `go mod tidy` (2026-08-09) corrected it from a stray `// indirect` marker to a proper direct `require` entry, matching that `bundle.go`/`verifier.go` import it directly. | `go build ./...`: exit 0; `go test ./... -race -cover -count=1`: exit 0, all packages pass; `internal/verifier`'s `bundle_test.go` proves canonicalization determinism and key-order independence (two JSON encodings of the same bundle with fields in different orders hash identically). |
| 2026-08-10 | Go (both modules, indirect) | `filippo.io/edwards25519` | v1.1.0 (GO-2026-4503, "invalid result or undefined behavior") | v1.2.0 | An independent audit flagged this and `golang.org/x/crypto` as pinned to versions with known OSV advisories, with reachability unconfirmed since `govulncheck` wasn't available at the time. Installed `govulncheck` (`go install golang.org/x/vuln/cmd/govulncheck@latest`) and ran it for real: neither `edwards25519` nor `x/crypto`'s advisories were in the *reachable* set for either module (both only appeared under "modules you require, but your code doesn't appear to call") — bumped anyway as good hygiene since both are trivial, low-risk indirect-dependency version bumps with no API surface WorkProof code touches directly. | `go build`/`gofmt -l .`/`go vet`/`go test ./... -race -cover` (go/) and `go build`/`gofmt -l .`/`go vet`/`go test ./...` (tools/): all exit 0. `govulncheck ./...` re-run after: `edwards25519`/`x/crypto` advisories no longer present in either module's output at all (not just moved to the unreachable bucket). |
| 2026-08-10 | Go (production, indirect) | `golang.org/x/crypto` (go/go.mod only — tools/go.mod was already on v0.54.0) | v0.50.0 (GO-2026-5019/5020/5021/5023/5033/5932, all `x/crypto/ssh*`/`openpgp`, unreachable from WorkProof's own call paths) | v0.54.0 | Same audit finding as the edwards25519 row above; bumped together via `go get`/`go mod tidy`. Transitively also bumped `golang.org/x/text` v0.36.0 → v0.40.0 in go/go.mod, which incidentally fixed a *reachable* vulnerability (GO-2026-5970, infinite loop on invalid input, reachable via `internal/extension`'s `processorutils.init` → `norm.Form.Properties` chain) that `govulncheck` surfaced as a genuine finding independent of the two originally flagged packages. | Same as above. `govulncheck ./...` reachable-vulnerability count in go/ dropped from 22 to 21 after this change (the x/text finding cleared; the remaining 21 are all Go standard-library CVEs tied to the go1.25.1 toolchain itself — not fixable via `go.mod`, tracked as a separate, not-yet-addressed gap, not silently folded into this row). |
